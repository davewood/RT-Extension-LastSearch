<%init>

# get the search main menu
my $m = Menu();

# add a child menu
$m->child(
    'menu_rt_extension_lastsearch',
    title => loc('Show Results'),
    path  => $session{'RT-Extension-LastSearch'},
);

</%init>
